
#include <bits/stdc++.h>

using namespace std;

const int max_sz = 1000;

bool valid_phrase[max_sz][max_sz];


bool indict (string s) {

    return true;
}

string word_split ()

int main() {

    string s;
    cin >> s;

    int slen = s.length();

    for(int fw_len = 1; fw_len <= slen; fw_len++) {

        if (indict (s.substr(0, fw_len)) and )
    }



}
